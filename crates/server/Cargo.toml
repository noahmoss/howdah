[package]
name = "howdah-server"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1.89"
howdah-core = { version = "0.1.0", path = "../core" }
nvim-rs = { version = "0.9.2", features = ["use_tokio"] }
tokio = { version = "1.52.3", features = ["full"] }
